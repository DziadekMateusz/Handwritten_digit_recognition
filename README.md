# Handwritten_digit_recognition
A handwritten digit classification system built with TensorFlow/Keras. The application preprocesses grayscale digit images (28×28 pixels), trains or loads MLP and CNN models, and evaluates their performance on a test dataset. During inference, it reports predicted probabilities, classification accuracy, average confidence, and prediction latency.
